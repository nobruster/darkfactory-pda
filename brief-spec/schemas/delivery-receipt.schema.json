{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://briefspec.dev/schemas/delivery-receipt.schema.json",
  "title": "BriefSpec delivery receipt",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "kind",
    "status",
    "delivery_id",
    "format",
    "destination",
    "content_sha256",
    "briefspec_version",
    "delivery_schema_version",
    "renderer_versions",
    "verification_level",
    "delivered_at"
  ],
  "properties": {
    "schema_version": { "const": "1.0" },
    "kind": { "const": "briefspec-delivery-receipt" },
    "status": { "enum": ["pending", "delivered"] },
    "delivery_id": { "type": ["string", "null"] },
    "format": { "type": "string", "minLength": 1 },
    "destination": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "locator"],
      "properties": {
        "kind": { "const": "local" },
        "locator": { "type": ["string", "null"] }
      }
    },
    "content_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "briefspec_version": { "type": ["string", "null"] },
    "delivery_schema_version": { "const": "1.0" },
    "renderer_versions": {
      "type": "array",
      "items": { "type": "string" },
      "uniqueItems": true
    },
    "verification_level": { "enum": ["structural", "resolved", "rendered", "delivered"] },
    "delivered_at": { "type": ["string", "null"], "format": "date-time" }
  }
}
