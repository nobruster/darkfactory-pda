# Managed by Brief-Spec.
interface:
  display_name: "Session Checkpoint"
  short_description: "Re-orient long sessions without losing evidence"
  default_prompt: "Use $session-checkpoint to orient me, teach the current model, or produce a spoken recap."
