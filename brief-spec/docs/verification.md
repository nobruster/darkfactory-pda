<!-- briefspec:verification:v1 version=0.5.0 -->
<!-- Generated by scripts/generate-verification.py; do not edit by hand. -->
# Brief-Spec 0.5.0 verification truth boundary

Updated: `2026-08-15`
Source basis: `872a85b110866dee52d7fae440d39fbbeace7883`
Canonical repository: [github.com/luanmorenommaciel/brief-spec](https://github.com/luanmorenommaciel/brief-spec)

This document separates implementation, validation, host evidence, and publication. A local or
globally installed candidate is not a public release.

## Current state

| Boundary | Evidence | Status |
| --- | --- | --- |
| Source candidate | Version `0.5.0` derived from `872a85b110866dee52d7fae440d39fbbeace7883` | Locally validated release candidate from an uncommitted worktree |
| Repository | `public` canonical repository | Canonical public repository confirmed |
| Latest GitHub release | `v0.2.0` | Published historical release |
| Local gates | 504 tests, 427 source checks, 504 wheel checks, 85.35% coverage on macOS / Python 3.14.6 | Lint, formatting, release verification, 504 tests, coverage, Chronicle E2E, browser, PDF, audio, video, and clean-room wheel/sdist gates pass locally |
| Hosted CI | [run 31784833997](https://github.com/luanmorenommaciel/brief-spec/actions/runs/31784833997) for `872a85b110866dee52d7fae440d39fbbeace7883` | executed-with-failures; steps executed: `true` |
| Live harness evidence | Sanitized disposable-repository results retained locally | Retained 0.5.0 baseline: Codex 8/8, Claude 8/8, OMP 4/4, Grok 4/4, and Kimi 4/4 passed; current exact-wheel synthetic probes pass but the full live matrix was not rerun on this worktree |
| Live release authorization | `release/live-e2e-evidence.json`; source fingerprint `975647c437867b419979db657413ce7e377ce1e19ad678eaeb18a3328781c6ec` | stale-for-current-worktree by `the retained live evidence predates the current Human Continuity, repository-layout, documentation, and portability changes` |
| GitHub `v0.5.0` | Release API and workflow state | not-published |
| PyPI core/PDF/audio | PyPI project/file lookup | not-published / not-published / not-published |
| PyPI Chronicle/video | Independent extension project/file lookup | not-published / not-published |
| Canonical schemas | `GitHub v0.5.0 release assets` plus `brief-spec-schemas.bundle.json` | built locally; not published |

The recorded hosted run executed repository jobs and is real platform evidence. It did not authorize release because the last run exposed four Windows portability assertions and a missing ffmpeg dependency in the macOS audio lane; both are fixed locally but not yet rerun on a committed exact revision. The current uncommitted candidate still requires a new exact-revision hosted run after it is committed. The retained five-host matrix remains useful baseline evidence, but exact-current-worktree live authorization is `stale-for-current-worktree` because the retained live evidence predates the current Human Continuity, repository-layout, documentation, and portability changes. Public release remains blocked on the exact-SHA live,
hosted, and account-owned publication gates.

## Harness evidence tiers

| Harness | Current claim |
| --- | --- |
| codex | `live-verified` |
| claude | `live-verified` |
| omp | `live-verified` |
| grok | `live-verified` |
| kimi | `live-verified` |
| copilot | `experimental` |
| cursor | `experimental` |
| goose | `experimental` |

`live-verified` means the deterministic installation controls and required authenticated matrix
passed for the retained `0.5.0` adapter baseline. It does not override the exact-worktree freshness
status above. `hold` is a visible unresolved condition. `experimental` is best effort and
non-blocking.

## Retained live-host matrix

| Harness | Passed/required | Local sanitized summary |
| --- | ---: | --- |
| codex | 8/8 | `.briefspec/live-e2e/ultimate-final-codex/summary.json` |
| claude | 8/8 | `.briefspec/live-e2e/ultimate-final-claude/summary.json` |
| omp | 4/4 | `.briefspec/live-e2e/ultimate-final-omp/summary.json` |
| grok | 4/4 | `.briefspec/live-e2e/ultimate-final-grok-verified/summary.json` |
| kimi | 4/4 | `.briefspec/live-e2e/ultimate-final-kimi/summary.json` |

Every passing scenario required the host hook, bounded contract, strict validation, equivalent
deterministic downloads, bundle and receipt verification, and an authorized-change check. The
summaries retain metadata and hashes, not credentials or raw transcripts. Grok's native gate uses
passive classification hooks, one bounded Stop repair, and a native read/edit allowlist in a
disposable repository; shell, web, memory, and subagents remain disabled.

The committed evidence file contains only sanitized decisions, artifact hashes, categorical failure
reasons, and hashed opaque session references. CI recomputes its release-source fingerprint and the
release authorization fails closed unless all required hosts pass.

## Candidate artifacts and rollout

| Boundary | Retained evidence | Status |
| --- | --- | --- |
| Distribution set | `.briefspec/fabric-candidate-final-20260815`; manifest `4236b514bf10ef719820d77c5d1eb3bf94603810d00e9ac836f17e6c5f7e9af5` | Ten core/PDF/audio/Chronicle/video wheel and sdist files pass Twine, SHA-256, resource projection, and exact-file wheel/sdist clean-room installation |
| Core wheel | SHA-256 `134c139c7cf52e566cdd2a85f00c09e31c434fdf5fd75bf3a17695fe9eb20500` | Installed globally from these exact bytes |
| Offline schemas | SHA-256 `bcf216a45136afd643055d4e343fe4564b7058c844d9817626c7c1a9bebc86b8` | Built and validated locally; not published |
| Global tool | Core, PDF, audio, Chronicle, and video | Core/PDF/audio 0.5.0 plus Chronicle/video 0.1.0 installed from the exact final local wheel set |
| Global doctors | User and detected project scopes | Codex, Claude, OMP, Grok, Kimi, and Cursor PASS; absent Copilot and lifecycle-limited Goose WARN; Chronicle self-doctor PASS |
| Intentional overrides | Receipt-owned paths only | Converge Claude and Nexo Codex PASS at 0.5.0 |
| Rollback | Byte-for-byte manifest verification | A current 54-path Brief-Spec-owned snapshot and full-stack wheel restore command are retained; the post-install rollback was not destructively rerun in this documentation pass |

The artifact manifest intentionally has no hosted source authorization: these bytes came from an
uncommitted local worktree. Hosted CI must rebuild once from the exact committed revision and the
release workflow must reuse those hosted bytes unchanged.

## Browser and optional-renderer gates

| Gate | Local result |
| --- | --- |
| Self-contained HTML | PASS at desktop and mobile widths with zero external requests |
| Playwright PDF | PASS for selectable text, fonts, metadata, geometry, page rendering, and deterministic bundle bytes |
| Offline macOS audio | PASS for MP3 decoding, duration, disclosure, source-script hash, and deterministic bundle bytes |
| Offline Chronicle video | PASS for offline 1600x900 H.264/AAC rendering, sidecars, manifest/receipt integrity, and source hashes |
| Explicit-consent OpenAI audio | Not run because OPENAI_API_KEY is absent; mocked coverage passed |

## Candidate trust corrections validated locally

- honest classification with negation masking, abstention, decision records, and reported-marker
  downgrade;
- zero-network and no-plugin verification defaults, SSRF-safe public-address policy, bounded files
  and archives, and no command execution;
- receipt-hash ownership with preservation of locally modified skills;
- deterministic runtime precedence and hermetic no-CLI/fake-CLI test lanes;
- status-first human renderings, portable release-hosted schemas, and an offline schema bundle;
- exact-SHA CI authorization that reuses tested bytes and blocks tag-time rebuilding;
- evidence-based harness maturity, with a retained full baseline for all five required hosts and
  an explicit exact-worktree freshness boundary.

## Account-owned release gates

- Commit the candidate and rerun the full live and hosted matrix on that exact revision
- Register PyPI Trusted Publishers for brief-spec and the PDF/audio renderers
- Register separate publishers for Chronicle/video only when those experimental packages are promoted
- Approve the v0.5.0 tag and public release after the exact-revision gates pass

The repository-owned implementation may prepare these gates but cannot truthfully mark them complete.
The release workflow must consume the exact candidate artifact produced by successful CI, publish
identical bytes through Trusted Publishing, verify PyPI/GitHub digests, then finalize GitHub.

Historical `v0.1.0` evidence remains in
[`verification-v0.1.0.md`](verification-v0.1.0.md) and is never promoted to current evidence.
