# Day 2 — Translator

**Seat:** Translator (SWE). AI-native. You do not have to know Java.  
**Converge:** Recap 0–1 as papers. Tonight **explain + run 2–3–4** (Seamwise at 3, Consensus is the barrier), then **Task-Spec (5)**. Task-Mesh internals so they understand. Pass **6–8** as factory is later (Day 4).  
**This file** is the brief for the Day 2 PPT. Build slides from the agenda below. Do not invent extra story.

**Deck:** [`presentation/d2-translator.html`](../presentation/d2-translator.html) — live, 34 slides. Identify by `data-act-name`. Seamwise kit: [`ebooks/ebook-seamwise.pdf`](../ebooks/ebook-seamwise.pdf).  
**Staff:** [`run/d2/`](../run/d2/README.md) — 12 beats, Day 1 mold: what they **type**. Stage lecture stays on the deck.  
**Brain:** [`brain/notebooklm/`](../brain/notebooklm/README.md) — **query**, do not rebuild. Nine packs. Type `06` is not in it.

**Swing:** Recap → SWE role → the engine → event-driven → **Agent Harness** → query brain + ontology → Converge 2–4 + Seamwise → Task-Spec → Task-Mesh internals → Debrief.

Public End-to-End numbers are **capabilities**. Clock puts Harness (Craft) **before** query (Floor) so an unbound agent never drafts ADRs.

Public gate: *the Translator can design the second plant and see the work as signed leaves.*  
Rings tonight: **harness.** HITL strong. You stay on the keys. You validate everything.  
**One Night.** There is no morning and no afternoon.

---

## Night 2 agenda (the projector)

Yesterday you **created** the seat, the brain, the graph, and the brief. Tonight you **recap**, name the **SWE**, name the **engine**, **query**, show the **harness**, then **Converge 2–4**, **Task-Spec**, and **Task-Mesh** internals.

`java2py` is a **nickname** for the Python translation engine — a second plant that reads the same raw bytes. It is **not** a port of `legacy/processor/src`.

- [ ] **End-to-End**
  1. *Recap* — the entire recap (yesterday’s close + transcript clarifications + 0–1 papers in [`docs/`](../docs/README.md)). Tight.
  2. *The SWE Role* — how the Translator behaves: on the keys, validates, does not have to know Java.
  3. *Python Translation Engine (`java2py`)* — the engine we will create. Same bytes, Python beside Java, first write Parquet. Not a source translator.
  4. *Event-Driven Architecture* — why: sense the drop, claim the batch, emit Parquet. Not a bus. Not Dagster.
  5. *Second Brain & Ontology Search* — query until there is enough to generate Converge 2–4 docs. Brain = concepts. OntoLayer = grain / keys / paid. Specs = what we build.
  6. *Agent Harness* — what the harness is (seat, tools, spawn). Bind is rails on it, shown here, not a sermon.
  7. *Converge & Seamwise (2, 3, 4)* — in depth, then execute. Structure → Decompose (Seamwise) → Consensus. **Owner signs.**
  8. *Task-Spec* — in depth, then see the leaves in `docs/tasks/`. No eval, no build.
  9. *Task-Mesh Engine* — internals, so they understand how signed leaves run. Loop factory (6–8) is Day 4 unless we explicitly run Type `01` tonight.

Show, then Hands-On. Do not flip a slide per prompt. Recap is **one act**, not three lectures.

---

## PPT blocks (build in this order)

Same mold as Day 1. Six HUD blocks. New file from `d1-archaeologist.html` chrome — do not tweak the old 32-slide d2. One idea per slide. Every block opens with a divider (except Opening).

**34 slides as built** (the brief asked for ~29). Four Hands-On boards only (A–D). Task-Mesh is Show (internals). Pass 6–8 factory is Day 4.

| Block | HUD | Mode | Agenda beats |
|---|---|---|---|
| **Opening** | 01 | title | — |
| **Stage** | 02–10 | keyboards down | 1 Recap · 2 SWE · 3 `java2py` · 4 Event-driven |
| **Craft** | 11–14 | Show + Hands-On **A** | 6 Agent Harness |
| **Floor** | 15–18 | Show + Hands-On **B** | 5 Brain & ontology |
| **Dig** | 19–24 | Show + Hands-On **C–D** | 7 Converge/Seamwise 2–4 · 8 Task-Spec · 9 Task-Mesh |
| **Debrief** | 25–29 | close | Research, then walk |

Craft is the machine **before** they query and sign — unbound agents do not draft ADRs. Floor query feeds Dig. Dig is explain then execute 2–4, then Task-Spec; Mesh stays internals.

### Slide list

| HUD | Block | Slide | Kind |
|---|---|---|---|
| 01 | Opening | Translator · `java2py` | title |
| 02 | Stage | Stage | divider |
| 03 | Stage | Recap · what closed | Recap |
| 04 | Stage | Recap · transcript + the two `docs/` files | Recap |
| 05 | Stage | The SWE Role | story |
| 06 | Stage | `java2py` · the problem | story |
| 07 | Stage | Two plants · first write is Parquet | story |
| 08 | Stage | Five-file package · not a port | story |
| 09 | Stage | Event-driven · why a file | story |
| 10 | Stage | Ingest seam · sense, claim, emit | story |
| 11 | Craft | Craft | divider |
| 12 | Craft | Show · Agent Harness (seat, tools, spawn) | Show |
| 13 | Craft | Bind is rails, not a prompt | Show |
| 14 | Craft | Hands-On **A** · fail-closed on `legacy/processor/` | HO |
| 15 | Floor | Floor | divider |
| 16 | Floor | Show · Second Brain (query for the plan) | Show |
| 17 | Floor | Show · OntoLayer + specs | Show |
| 18 | Floor | Hands-On **B** · query until 2–4 has evidence | HO |
| 19 | Dig | Dig | divider |
| 20 | Dig | Show · Converge 2–4 + Seamwise (lanes at 3) | Show |
| 21 | Dig | Hands-On **C** · Structure → Decompose → Consensus | HO |
| 22 | Dig | Show · Task-Spec | Show |
| 23 | Dig | Hands-On **D** · the leaf in `docs/tasks/` | HO |
| 24 | Dig | Show · Task-Mesh internals | Show |
| 25 | Debrief | Debrief | divider |
| 26 | Debrief | In hand | close |
| 27 | Debrief | Research | close |
| 28 | Debrief | Next | close |
| 29 | Debrief | Tomorrow | silent |

Hands-On clone Day 1 Execute 01–04: chip, `run/d2/` path, look-up / do not. No slide per prompt.

### Creation moves (one block per pass)

1. **Chrome** — copy Day 1 HUD, tokens, Hands-On tile. Empty deck shell. HUD `01`/`29`.
2. **Opening + Stage (01–10)** — story only. Review before any Hands-On.
3. **Craft (11–14)** — Harness Show + board A.
4. **Floor (15–18)** — query Show + board B.
5. **Dig (19–24)** — kits Show + boards C–D + Mesh Show.
6. **Debrief (25–29)** — Research, Next, silent Tomorrow.

Do not start pass 3 until 01–10 read as one story. Do not add a fifth Hands-On for Mesh unless we decide to run Type `01` on the mesh tonight.

---

## Intent

Everyone still arrives AI-native. Nobody in the room has to know Java. The Second Brain and the specs teach the **job** of the Java plant — privacy boundary, sanitize, overpunch, refuse — so a Python SWE can translate it. They leave able to:

1. Query the Second Brain and name the Java plant in concepts (not in source). Cite a page.
2. Restate the Day 1 tech-spec and the Type `01` inbound: what we will build, what we will not.
3. **Bind the harness.** Prove an agent cannot write `legacy/`, `contracts/`, `gen/`, or `infra/`.
4. Write grounding ADRs from brain + graph + specs — what is true, never how to code (Pass 2).
5. Cut the ingest → landing seam. Steel thread = Type `01` (Pass 3).
6. Run Consensus as a different voice. Owner signs. **That is the barrier** (Pass 4). If they will not sign, there is no parser.
7. Author eval-backed Task-Specs. No eval, no build (Pass 5).
8. Understand Task-Mesh internals (how signed leaves run). **Do not** run factory Loop (6–8) unless you explicitly decide to execute Type `01` on the mesh tonight.

They do not port `legacy/processor/src`. They do not stand up dlt, dbt, or Dagster. They do not serve Gold. They do not open Type `06`. They do not repair `173.44`. They do not write Parquet back to SFTP.

---

## Stage — chapters for the PPT

Use the Night 2 agenda order. One idea per slide.

### 1. Recap (the entire recap — one act)

- MATCHED. Nine-pack brain. OntoLayer. BRD + tech-spec. **Nobody signed Consensus.**
- Transcript: [`transcripts/tr-d1-archaeologist.cc.vtt`](../transcripts/tr-d1-archaeologist.cc.vtt) — clarify what they actually stumbled on. Not a generic recap.
- Open the papers: [`docs/brd-type-01-card-settlement.md`](../docs/brd-type-01-card-settlement.md), [`docs/tech-spec-type-01-card-settlement.md`](../docs/tech-spec-type-01-card-settlement.md). Look. Do not rerun 0–1.

### 2. The SWE Role

- Translator = SWE on this plant: grammar, privacy, money, the first write.
- Stays on the keys. Validates. Does not have to *be* a Java engineer.
- Query, then design, then (only after the sign) see work as Task-Specs.

### 3. Python Translation Engine (`java2py`)

- Nickname for the **second plant**, not a Java→Python source converter.
- Same raw SFTP bytes. Java still writes CSV. Python writes Parquet.
- You may look at Java to name a concept. You may not copy Java to compute an answer.

### 4. Event-Driven Architecture

- Why: a file lands overnight. Sense, claim, emit. That is the ingest seam.
- Not a message bus. Not Dagster (Day 4). Not dlt (Day 3).

### 5. Second Brain & Ontology Search

- Query until Converge 2–4 has evidence. Cite a page / a routine.
- Brain = privacy, overpunch, refuse, sanitize (concepts).
- OntoLayer = grain, keys, which procedure writes paid.
- Specs + `contracts/` = what we build and what wins.

### 6. Agent Harness

- The harness is the machine: workspace, tools, spawn.
- Bind = rails on that machine (paths, privileges). A polite prompt is not a fence.
- Show fail-closed on `legacy/processor/` if you touch the fence tonight.

### 7. Converge & Seamwise (2, 3, 4)

Yesterday 0–1 are **look**. Tonight you **explain in depth, then run** 2–4 on **one seam**.

Papers live in [`docs/`](../docs/README.md) — this repo’s Converge home, not `cvg/docs/`.

| Pass | Name | Tonight | File |
|---|---|---|---|
| 0 Capture | the BRD | Look. Already written. Do not reopen unless empty | [`docs/brd-type-01-card-settlement.md`](../docs/brd-type-01-card-settlement.md) |
| 1 Intent | the tech-spec | Look. Restate it. Do not pick a stack that is not in it | [`docs/tech-spec-type-01-card-settlement.md`](../docs/tech-spec-type-01-card-settlement.md) |
| **2 Structure** | ADRs | What is true. Brain + graph + specs. Never how. Ten questions closed or parked | `docs/adrs/` + `docs/CONTEXT.md` |
| **3 Decompose** | seams | Seamwise attaches. Ingest → landing is the lane. Steel thread = Type `01` | `docs/seams.md` |
| **4 Consensus** | the barrier | Different voice. FIXED or ACCEPTED. **Owner signs.** No sign, no parser | `docs/consensus.md` |
| **5 Tasking** | leaves | Task-Spec in depth, then the file. One leaf, one eval, `signed_off` false | `docs/tasks/` |
| 6 Register | issues | Opt-in. **Later** (factory) | — |
| **7 Bind** | the fence | Shown with the Agent Harness. Epoch / Loop factory is Day 4 | harness, not a doc |
| **8 Loop** | Task-Mesh | Internals tonight so they understand. Unattended factory is Day 4 | mesh / later `modern/landing/` |

dlt → Gold is Day 3. Dagster + Pass 6–8 factory is Day 4. Type `06` is Friday.

### 8. Task-Spec

In depth. Then look at `docs/tasks/`. No eval, no build.

### 9. Task-Mesh Engine

Internals: how signed leaves are executed, retried, settled. Understanding is tonight. Lights-out Loop is Day 4 unless we explicitly run Type `01` on the mesh before we walk.

---

## Craft — Bind the harness

The seat from Day 1 still sits. Do not reinstall Oh My Pi. **Tonight we bound it.**

House stack (unchanged): Oh My Pi → OpenRouter → a workspace (CMUX, ORCA, Super Engineering, or BYO) → DeepSeek. We grade gates, not the vendor.

### Bind (required — the Night does not start without it)

| Rule | Why |
|---|---|
| Bind is on before any `modern/` write | Paths and privileges, not vendor loyalty |
| Frozen folders refuse writes | `legacy/`, `contracts/`, `gen/`, `infra/` |
| Prove fail-closed in public | Touch `legacy/processor/` → must refuse |
| Any agent that honors Bind may sit | We grade the fence, not the IDE |
| Decimal, never float | One cent is the whole class |
| No Parquet on reject | Expected terminal, not missing data |
| Do not read Java to compute a result | Observation only. Specs + contracts judge |

If Bind is not proven, **stop**. Do not start Structure with an unbound agent.

### Second Brain tonight

Query. Do not rebuild. **Same nine packs as Day 1.** Do not upload a tenth file (tech-spec, ADRs, Java, `modern/`, Type `06`).

Ask it to teach **concepts** the Java plant already lives:

- What is Type `01` physically? Overpunch, H/D/T, the lie.
- What must never leave sanitize? PAN, CPF.
- What does “done” mean for accept vs refuse?
- Which meeting contradicts the layout?

Cite a page. A fluent answer with no source is a failure. Specs, OntoLayer, and `docs/` answer what we build — not a new NotebookLM source.

### Specs tonight

| Source | Job |
|---|---|
| Day 1 tech-spec | What we said we would build |
| `spec/type-01-…` inbound | What the customer mailed |
| `contracts/types/01-…` | The judge |
| OntoLayer | Grain, keys, which procedure writes paid |

Inbound prose does not outrank the contract. The graph does not replace a signed ADR.

### Kits tonight

| Kit | Job tonight |
|---|---|
| **Converge** | Pass **2–4** then **5**. Still does not write product code. Factory 6–8 is Day 4 |
| **Seamwise** | Attaches at Decompose. One lane: ingest → landing |
| **Task-Spec** | Attaches at Tasking. One leaf, one eval |
| **Brief-Spec** | Names the hour: decide, then implement, then review |

---

## Floor — one Night, in order

Instructor drives, room types. Progressive. Do not jump. Staff: [`run/d2/`](../run/d2/README.md) — **12 beats**, same mold as Day 1 (verbatim prompts + proof). Opening + Stage (HUD 01–10) are the **deck** — no run file per lecture slide.

| Slice | Beats | Deck | Look up |
|---|---|---|---|
| **A · Recap** | [`01`](../run/d2/01-prompt-status.md)–[`02`](../run/d2/02-prompt-papers.md) | HUD 03–04 | MATCHED · papers restated |
| **B · Harness** | [`03`](../run/d2/03-prompt-harness.md)–[`04`](../run/d2/04-fail-closed.md) | HUD 12–14 | fail closed |
| **C · Query** | [`05`](../run/d2/05-query-brain.md)–[`06`](../run/d2/06-query-graph.md) | HUD 16–18 | page + routine |
| **D · 2–4** | [`07`](../run/d2/07-prompt-kits.md)–[`10`](../run/d2/10-consensus.md) | HUD 20–21 | **sign** |
| **E · Task-Spec** | [`11`](../run/d2/11-taskspec.md) | HUD 22–23 | leaf + eval |
| **Close** | [`12`](../run/d2/12-research.md) | HUD 27–29 | three citations |

No sign → skip 11. Task-Mesh is Show (HUD 24). No landing Loop.

---

## What you will hold in your hand at the end

- Recap closed: MATCHED, papers in `docs/`, transcript clarifications.
- `java2py` named as a second plant, not a port.
- Harness shown. Fail-closed on frozen folders.
- Citations enough for Pass 2–4. ADRs, seams, **Consensus signed.**
- One Task-Spec leaf with an eval in `docs/tasks/`. `signed_off` false.
- Task-Mesh internals understood. **No factory Loop.** No `modern/` required.
- The same Second Brain and OntoLayer you queried — not rebuilt.
- No dlt. No Gold. No Dagster. No Type `06`. No repair of `173.44`.

---

## Debrief

- Design is done when the owner signs, not when the slide looks complete.
- You did not need to *be* a Java engineer. You needed the concepts, the specs, and the fence.
- Tonight you hold **leaves**, not Parquet. Landing is Day 3+ if the mesh/factory later writes it.
- Tomorrow the constructor **registers** landing only if those files exist. dlt does not re-parse.
- The agent is a seat. **Bind is the law.** The eval is the judge. The mesh is how signed leaves run.

---

## Homework

1. Re-read the signed brief, the ADRs, and the seam list. Restate them without the file open.
2. The Task-Spec leaf still has an eval. `signed_off` is still false.
3. Skim Types `02`–`04` inbound **layouts only** (brain packs 04–06). Do not start Type `05`.
4. Read the golden-match two questions in [`plans/modern.md`](../plans/modern.md). You attach them when landing exists; you do not rewrite the referee.

---

## Research

Query tonight for Day 3. Same notebook. Do not rebuild it.

1. **OntoLayer** — "What grain and keys must dlt register, and what is the reconciliation grain?"
2. **Second Brain** — architecture meeting: what does the new plant write after the independent parser, and must it call Java? Cite the page.
3. **plans/modern.md** — the golden-match two questions. Tomorrow you attach them.

---

## Exit criteria

- [ ] Bind proven: agent cannot write frozen folders.
- [ ] Pass 2 ADRs exist (ten modern questions closed or parked). Brain / graph / specs cited as evidence, not as the ADR.
- [ ] Pass 3 seams match Days 3–4. Steel thread is Type `01`.
- [ ] Pass 4 Consensus signed. Every objection FIXED or ACCEPTED.
- [ ] Pass 5: one leaf, one eval, in `docs/tasks/`. `signed_off` false. No product code required.
- [ ] Task-Mesh internals named. Factory Loop (6–8) **not** required tonight.
- [ ] First write (when it happens) is landing Parquet, not SFTP — in the ADRs, not necessarily on disk.
- [ ] No dlt / dbt / Dagster / FastAPI required tonight.
- [ ] Type `06` still absent.

## Day 2 is not

- Not Day 1 (do not rebuild the Second Brain or re-index OntoLayer unless they are actually down).
- Not “learn Java by pasting `legacy/processor/src`.” Grasp concepts. Observe the plant.
- Not “parser first, ADRs later.”
- Not Day 3 (no lakehouse, no Gold, no golden-match attach).
- Not Day 4 (no Dagster, no Type `05` pill).
- Not “we all agree, ship it” without an adversary pass.
- Not a repair of `173.44`.
- Not CSV on SFTP as a modern input.
