---
id: T-20260826-type-01-gold
title: Type 01 Gold at paid grain batch_id + currency
status: ready
format_version: 3
profile: standard
effort: S
budget_iterations: 10
agent: claude
parent: docs/seams.md
depends_on:
  - T-20260826-type-01-silver
supersedes: (none)
touches_paths: []
creates_paths:
  - modern/dbt/models/gold/gold_card_settlement_reconciliation.sql
source_note: "ADR 0009 Gold grain; OntoLayer paid table"
created: 2026-08-26T18:00:00Z
tags: [type-01, gold, dbt]
owner: Luan Moreno
priority: P1
severity: financial-critical
due_date: (none)
precondition: "Type 01 Silver model exists"
blocked_reason: (none)
security_class: restricted_synthetic_pii
source_action_item: (none)
tracker_ref: (none)
execution_backend: claude
signed_off: true
signed_off_by: luanmorenomaciel
signed_off_at: 2026-08-29T00:58:15Z
accepted: true
accepted_by: luanmorenomaciel
accepted_at: 2026-09-10T19:05:09Z
evidence_refs: []
signed_off_sig: hmac-sha256-v3:d90e2e61:1c267173055530758785ef59e531c0d2695108104362dbaaff8c4cbea806e155
accepted_tier: 2
accepted_attempt_id: 21c9321d-037e-5335-8d15-c4281c04e382
accepted_authorization_ref: hmac-sha256-v3:d90e2e61:1c267173055530758785ef59e531c0d2695108104362dbaaff8c4cbea806e155
acceptance_record_digest: sha256:1024f356e99d12536ccd5b33308367938cbbbf8ccd5b69782c3851c9c1613c7a
---

# Type 01 Gold at paid grain batch_id + currency

## Context

This leaf owns one artifact in the Type 01 steel thread. Legacy is the referee,
never the teacher: the modern side is built from `contracts/` alone. The eval
executes the artifact rather than asserting it exists.

## Goal

Gold is the governed recon that may later be served. Grain is
`batch_id` + `currency`. Rebuild from landing. Do not read PostgreSQL
to compute the number.

## Behavior

- **B-1** — One Gold row per (`batch_id`, `currency`) for accepted landing.
- **B-2** — `valid-minimal` is `MATCHED`, applied_net `173.45`, amount_delta `0.00`.
- **B-3** — SQL does not reference `postgres`, `reporting.`, or Java.

## Success Criteria

`eval_3` **executes** against the real artifact this leaf owns.

```bash
ROOT="$(git rev-parse --show-toplevel)"

eval_1() {
  test -d "$ROOT/cvg/tasks" || return 1
}

eval_2() {
  test -x "$ROOT/modern/.venv/bin/python" || return 1
}

eval_3() {
  cd "$ROOT" || return 1
  ./modern/.venv/bin/python - <<'PYEOF'
import duckdb, pathlib
c = duckdb.connect(str(pathlib.Path.cwd()/"modern/lakehouse/ducklake/northwind_modern.duckdb"), read_only=True)
r = c.execute("select currency, applied_net_amount, status from gold.gold_card_settlement_reconciliation where batch_id='B202607230000001'").fetchone()
assert r and str(r[1]) == "173.45" and r[2] == "MATCHED", r
print("eval_3 OK gold", r)
PYEOF
}
```

## Validation Card

```yaml
success_criteria:
  - id: eval_3
    description: EXECUTES the artifact this leaf owns
    runnable: bash
    check_type: deterministic
    verifies: [B-1]
    terminal: true
    expected_duration_sec: 60
  - id: eval_1
    description: Gold SQL is paid grain and does not read Postgres
    runnable: bash
    check_type: deterministic
    verifies: [B-1, B-3]
    terminal: true
    expected_duration_sec: 5
  - id: eval_2
    description: Clean local rebuild yields MATCHED 173.45; lie has no Gold
    runnable: bash
    check_type: deterministic
    verifies: [B-2]
    terminal: true
    expected_duration_sec: 90

retry_policy:
  max_iterations: 10
  circuit_breaker_no_progress: 3
  on_terminal_failure: park_with_context

agent_contract:
  version: 2
  read: [intent, behavior, contract, guardrails, operations]
  produce: [code]
  required_tools: [git, bash]
  timeout_minutes: 30
  sandbox_type: host
  output_artifacts: []
  mcp_dependencies: []
  emit: [pass, fail, retry_with_reason, parked_with_context]
  backend_metadata: {}
```

## Exit Check

```bash
eval_1 && eval_2 && eval_3
```

## Do-Not-Touch

- `legacy/`
- `contracts/`
- `gen/`
- `infra/`
- `validation/golden-match/golden_match.py`

---

## Rollback Plan

Additive. Revert the declared paths with `git checkout --` / `git rm`.
Never revert a frozen tree to make a gate pass.

---

## Observability Hooks

Watch the artifact this leaf owns; any unexplained financial delta blocks the
release gate.

---

## Open Questions

(none — the contract and the ADRs fix the grain and the money rule.)

---

## Anti-Patterns

- Don't weaken the referee. Don't rewrite `expected/`. Don't patch `legacy/`.
