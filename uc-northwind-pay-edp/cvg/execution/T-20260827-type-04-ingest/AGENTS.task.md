# Task brief — T-20260827-type-04-ingest

> Generated by Pass 7 · Bind. **The signed Task-Spec is the only instruction source** — this brief points at it, never replaces it. It holds identifiers, not content: open what you need, when you need it.

- **Spec (canonical):** `cvg/tasks/T-20260827-type-04-ingest.md`
- **Contract:** `cvg/execution/T-20260827-type-04-ingest/execution-profile.yaml`
- **Epoch:** `T-20260827-type-04-ingest@a694b5156335`

## Goal

Author (and later execute) one Type 04 five-file package so `valid-minimal` may emit `modern/landing/` Parquet and `DF-SOURCE-004` emits **zero** Parquet (`SOURCE_CONTROL_NET_MISMATCH`). Decimal.

## You may write ONLY these paths

- `modern/ingestion/src/northwind_pay/types/04-ted-transfer-settlement/handler.py`
- `modern/ingestion/src/northwind_pay/types/04-ted-transfer-settlement/model.py`
- `modern/ingestion/src/northwind_pay/types/04-ted-transfer-settlement/parser.py`
- `modern/ingestion/src/northwind_pay/types/04-ted-transfer-settlement/schema.py`
- `modern/ingestion/src/northwind_pay/types/04-ted-transfer-settlement/writer.py`

**Never touch:**

- `contracts/`
- `gen/`
- `infra/`
- `legacy/`
- `validation/golden-match/golden_match.py`

## Done means

Done is not your judgement — it is this command exiting zero from a clean checkout:

```bash
eval_1 && eval_2 && eval_3
```

## Boundaries

- network: **deny** · external writes: **deny**
- enforcement on `claude`: **detect** (`prevent` = blocked before it happens; `detect` = caught at settlement)
- authority closes on: settle, block, budget_exhausted, epoch_change — a new epoch requires a fresh bind

## Before you settle

```bash
python3 skills/task-to-runtime-contract/scripts/check-path-policy.py --profile cvg/execution/T-20260827-type-04-ingest/execution-profile.yaml --base "$CVG_BASE_REF"
```

If you need something outside this scope, **stop and request a re-bind**. Widening scope yourself invalidates the seal and the work will be rejected.
