# Task brief — T-20260826-type-01-landing-emit

> Generated by Pass 7 · Bind. **The signed Task-Spec is the only instruction source** — this brief points at it, never replaces it. It holds identifiers, not content: open what you need, when you need it.

- **Spec (canonical):** `cvg/tasks/T-20260826-type-01-landing-emit.md`
- **Contract:** `cvg/execution/T-20260826-type-01-landing-emit/execution-profile.yaml`
- **Epoch:** `T-20260826-type-01-landing-emit@dc01647651a7`

## Goal

Finish the Type 01 five-file package (`schema` / `writer` / `handler`) so `valid-minimal` publishes deterministic Parquet under `modern/landing/` (net 173.45 shape) and `df-source-001` emits **zero** Parquet. Do not write `legacy/`, `contracts/`, `gen/`, or `infra/`.

## You may write ONLY these paths

- `modern/ingestion/src/northwind_pay/common/parquet.py`
- `modern/ingestion/src/northwind_pay/emit.py`
- `modern/ingestion/src/northwind_pay/types/01-card-settlement/handler.py`
- `modern/ingestion/src/northwind_pay/types/01-card-settlement/model.py`
- `modern/ingestion/src/northwind_pay/types/01-card-settlement/schema.py`
- `modern/ingestion/src/northwind_pay/types/01-card-settlement/writer.py`

**Never touch:**

- `contracts/`
- `gen/`
- `infra/`
- `legacy/`
- `validation/golden-match/golden_match.py`

## Done means

Done is not your judgement — it is this command exiting zero from a clean checkout:

```bash
eval_1 && eval_2 && eval_3
```

## Boundaries

- network: **deny** · external writes: **deny**
- enforcement on `claude`: **detect** (`prevent` = blocked before it happens; `detect` = caught at settlement)
- authority closes on: settle, block, budget_exhausted, epoch_change — a new epoch requires a fresh bind

## Before you settle

```bash
python3 skills/task-to-runtime-contract/scripts/check-path-policy.py --profile cvg/execution/T-20260826-type-01-landing-emit/execution-profile.yaml --base "$CVG_BASE_REF"
```

If you need something outside this scope, **stop and request a re-bind**. Widening scope yourself invalidates the seal and the work will be rejected.
