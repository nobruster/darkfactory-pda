# Swimlanes — this plant only

Project **this** plant. Do not copy `assurance` / `foundation` / `models`.

| Folder | Seam | Owner | Papers |
|---|---|---|---|
| `ingest-landing/` | ingest → landing | Translator (SWE) | `docs/seams.md` seam 1 |
| `dlt-gold/` | dlt → Gold | Constructor (DE + AE) | `docs/seams.md` seam 2 |
| `orchestrate-serve/` | orchestrate + serve | Orchestrator | `docs/seams.md` seam 3 · ADR 0012–0013 |

Type `06` (Night 5) rides the same three lanes; its cut is
`docs/seams-type-06.md` and its sign is `docs/consensus-type-06.md`.

Canonical Task-Specs stay in `docs/tasks/`. Linear / `_linear-board.md` is the board, not the judge. Chat does not settle.
