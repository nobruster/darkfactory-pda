# 10 · The estate

- Slide: DIG · Execute 10–11
- Slice: **C · Read**
- Who: instructor points, room looks — keyboards stay down until 11
- Next: [`11-prompt-estate.md`](11-prompt-estate.md)

## Do

Do not dump Java. Do not `tree` the whole repo. Point at the folders that carry truth:

| Folder | Say this |
|---|---|
| `spec/` | inbound — mail, meetings, Type 01 samples. Not the judge. |
| `contracts/` | the judge. Frozen. Outranks code. |
| `gen/` `infra/` `legacy/` | frozen plant. Do not write. |
| `docs/` | Converge paper trail. Capture + Intent write here tonight. Not the HTML manuals. |
| `brain/` | Second Brain pack. Upload tonight. Query all week. Do not dump Java into it. |
| `ontology/` | OntoLayer over live Postgres. Ask without, then with. |
| `validation/` | oracle. Recomputes. Never repairs. |
| `Makefile` | the operator surface every agent reads. |
| `evidence/` | the run. Gitignored. Open in the **terminal**. |
| `modern/` | not here. `ls modern` must fail. |

Open one inbound file as mail, not as a spec:

```bash
ls spec/estate/
# cover.md  mail/  meetings/  policies/
```

Do not treat `spec/estate/cover.md` as `contracts/`.

## Proof

The room can name inbound, judge, freeze, and “modern is not here” without opening Java.

## If fail

If someone starts walking `legacy/processor/src`, stop. That is a tour. Go back to the four truths on the slide.
