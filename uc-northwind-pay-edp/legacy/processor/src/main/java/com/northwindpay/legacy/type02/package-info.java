/**
 * Type 02 UTF-8 instant-payment event parsing and sanitization.
 *
 * <p>The package enforces escape-aware pipe parsing, exact money and timestamp
 * rules, CPF/CNPJ validation, a dedicated HMAC domain, whole-output privacy
 * checks, and aggregate source controls.
 */
package com.northwindpay.legacy.type02;
