# Contributing

Humans and coding agents follow the same contract: [AGENTS.md](AGENTS.md).

Run `make check` before considering a change done. Do not rewrite frozen
artifacts under `release/<version>/`, `.taskspec/acceptance/`, or `tasks/done/`.

Security reports belong in [SECURITY.md](SECURITY.md), not in a public issue.
